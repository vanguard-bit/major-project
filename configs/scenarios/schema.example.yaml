# Documented scenario schema example. Excluded from metrics discovery.
schema_version: "1.0.0"
id: schema-example
suite: crm
platform_style: generic-crm
description: >
  Example-only fixture documenting the scenario YAML contract. Not executed
  as part of the labeled corpus.
target:
  name: schema-example
  base_url: http://mock.invalid/
  integration_sync_url: http://integration.invalid/sync
  audit_base_url: http://mock.invalid/
  expected_endpoints:
    - /api/v1/customers
  sensitive_markers:
    - billing_email
    - tax_id
exchanges:
  - phase: baseline
    method: GET
    path: /api/v1/customers
    response_body:
      - customer_id: cust-001
  - phase: mutated
    method: GET
    path: /api/v1/customers
    response_body:
      - customer_id: cust-001
expected_labels: []
