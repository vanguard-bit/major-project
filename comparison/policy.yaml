schema_version: "1.0.0"
name: comparison-target
environment: comparison
base_url: http://127.0.0.1:8080/
integration_sync_url: http://127.0.0.1:8080/health
audit_base_url: http://127.0.0.1:8080/
auth_type: static_token
token_config:
  token: "[REDACTED]"
expected_endpoints:
  - /health
  - /api/v1/items
  - /api/v1/items/item-1
sensitive_markers:
  - billing_email
description: >
  AIT expected-endpoint/field policy for the isolated comparison target.
